    <nav class="navbar navbar-default navbar-expand-lg fixed-top">
    <div class="container flex justify-content0-between align-items-center">

      <span></span>
     
        <h6 class="title"><%= @group.name.upcase %></h6>


      <div></div>


    </div>
  </nav>
<div class="d-flex justify-content-between align-items-center bg-white p-2">

    

        <div class="d-flex justify-content-start align-items-center gap-3">
        <%= link_to root_path do  %> 
          <i class="fa-solid fa-chevron-left icon" ></i>
        <% end %>
              <img src="<%= @group.icon %>" height="50" width="50" class="rounded-circle">
              <div class="flex-column d-flex justify-content-between mt-4">
                <h6 class="pb-3 text-black-60"><%= @group.name.capitalize %></h6>
              </div>
            </div>

        <p class="text-black">$<%= @group.entities.sum(:amount) %></p>
</div>

<div class="mb-5">
<% if @group.entities.empty? %>

    <div class="d-flex flex-column justify-content-center align-items-center m-5">

        <p>Transaction is empty.</p>
        <%= link_to 'Add Transaction', new_entity_path(group_id: @group.id), class: 'btn btn-primary px-3' %>

     </div>
<% else %>




 <% @group.entities.order(created_at: :desc).each do |group|%>

          <div class="d-flex justify-content-between align-items-center bg-white p-2 gap-6 m-2 shadow-sm" >

            <div class=" w-100">

              <div class="d-flex justify-content-between align-items-center mt-4 mb-4 w-100">
                <h6 class=" text-black-60"><%= group.name %></h6>
                <div class="fw-bold">$<%= group.amount %></div>
              </div>

              <p class="text-black-50 time"><%= group.created_at.strftime("%Y-%m-%d") %></p>

            </div>
            
        </div>

    <% end %>
<% end %>



</div>
  <div class="position-fixed bottom-0 w-100 category-button ">
    <div class="d-flex justify-content-center align-items-center">

     <%= link_to 'Add Transaction', new_entity_path(group_id: @group.id), class: 'btn w-100 m-2 add-category' %>
    </div>
</div>



