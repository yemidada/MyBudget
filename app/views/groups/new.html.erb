<%= form_with model: @group, url: groups_path, class: "form" do |f| %>
   
  <nav class="navbar navbar-default navbar-expand-lg fixed-top">
    <div class="container flex justify-content0-between align-items-center">

      <div></div>
     
      <h6 class="title">ADD CATGEORY</h6>

      <div class="header_button">
        <%= f.submit "Submit" %>
      </div>
    </div>
  </nav>

      <div class="d-flex justify-content-start align-items-center bg-white mav-header">

        <%= link_to root_path do  %> 
          <i class="fa-solid fa-chevron-left icon" ></i>
        <% end %>

  
    </div>
 
<section class="auth m-3 shadow-sm">

<% if @group.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@group.errors.count, "error") %> prohibited this model from being saved:</h2>
    <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= f.text_field :name, autofocus: true, autocomplete: "name" , placeholder: "Name" %>
    
  </div>

  <div class="field">
    <%= f.file_field :icon, autofocus: true, autocomplete: "icon", placeholder: "Icon" %>
  </div>
</section>
<% end %>
